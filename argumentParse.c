//// Created by louda on 11/15/20.//#include <stdio.h>#include <stdlib.h>#include <unistd.h>#include <string.h>#include "argumentParse.h"int parseArg(int argc, char **argv){    int pcap = 0;    int sniff = 0;    int returnVal = WRONG_ARG;    int index;    int c;    while((c = getopt(argc,argv,"r:i:")) != -1){        switch (c){            case 'r':                returnVal = PCAPNG;                pcap = 1;                if (!strcmp("-i",optarg))                    returnVal = WRONG_ARG;                break;            case 'i':                sniff = 1;                returnVal = SNIFF;                if (!strcmp("-r",optarg))                    returnVal = WRONG_ARG;                break;            case '?':                returnVal = WRONG_ARG;                break;            default:                break;        }    }    if (sniff && pcap)        returnVal = WRONG_ARG;    return returnVal;}